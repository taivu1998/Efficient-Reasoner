# Core ML Libraries
numpy<2.0.0
torch>=2.4.0
transformers>=4.44.0
trl>=0.11.0
peft>=0.12.0
bitsandbytes
accelerate>=0.27.0

# Unsloth for efficient training (install separately if issues)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth

# Data & Utilities
datasets
pyyaml
tqdm
scipy

# Evaluation & Visualization
matplotlib
seaborn

# Development
flake8
pytest

# Optional: vLLM for faster inference (requires CUDA)
# Uncomment if you have CUDA and want faster inference
# vllm
